services:
  api:
    image: karao-api:rocm
    build:
      context: .
      dockerfile: Dockerfile.api.rocm
    ports:
      - "3011:3001"
    volumes:
      - /mnt/gloubinours2/lrclib-db-dump-20260122T091251Z.sqlite3:/lrclib.sqlite3
      - /mnt/gloubinours2/karao_data:/data
    # Required for ROCm/GPU access
    devices:
      - "/dev/kfd:/dev/kfd"
      - "/dev/dri:/dev/dri"
    group_add:
      - video
    #   - render
    security_opt:
      - seccomp:unconfined
    environment:
      - LRCLIB_DB=/lrclib.sqlite3
      - DATA_DIR=/data
      - PORT=3001
      - HSA_OVERRIDE_GFX_VERSION=10.3.0
    restart: unless-stopped

  web:
    image: karao-web
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3010:3000"
    environment:
      - API_URL=http://api:3001
      - ORIGIN=https://doxx.fr
    depends_on:
      - api
    restart: unless-stopped

networks:
  default:
    name: karao
