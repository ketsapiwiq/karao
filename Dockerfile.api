FROM oven/bun:1

WORKDIR /app

RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*

RUN pip3 install --break-system-packages yt-dlp demucs torchcodec

COPY package*.json ./
RUN bun install

COPY api-server.ts ./

EXPOSE 3001

CMD ["bun", "run", "api-server.ts"]